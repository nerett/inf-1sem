#include <stdlib.h>
#include <stdio.h>

int cmp_string_ints(const void * p1, const void * p2)
{
	int num1 = ( int )( *(  char* )( p1 ) );
	int num1 = ( int )( *( char* )( p2 ) );

	return num1 - num2;
}

int main()
{
	char* 
	return 0;
}
